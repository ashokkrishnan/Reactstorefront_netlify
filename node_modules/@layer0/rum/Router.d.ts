import RouteCallbackParams from './RouteCallbackParams';
export default class Router {
    private readonly routes;
    match(pattern: string, callback: (params: RouteCallbackParams) => void): this;
    getPageLabel(pageURL: string): undefined;
}
